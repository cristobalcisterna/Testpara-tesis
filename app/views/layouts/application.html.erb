<!DOCTYPE html>
<html>
  <head>
    <!-- CAMBIAR NOMBRE APLICACIÓN BASE
    PARA CAMBIAR EL TITULO EN LAS DISTINTAS VISTAS UTILIZAR
    <% #content_for(:title) do %>
      Internal Positions
    <% #end %>
    -->
    <title>
      <% if content_for?(:title) %>
        <%= yield :title %>
      <% else %>
        Sistema de Gestion de inventario
      <% end %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= javascript_pack_tag "application", 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'app_vue',media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'app_vue', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="app">
      <div style="min-height: 100vh;" class="has-background-white-ter">
        <%=render 'partials/navigation' %>
        <% if notice || alert %>
          <div class="container">
            <div class="columns has-text-centered">
              <div class="column"></div>
              <div class="column is-narrow">
                <br>
                <div class="notification is-info" style="padding: 0.5rem;background-color:#8c67ef">
                  <button class="delete" onclick="this.parentElement.style.display='none'" ></button>
                  <p class="notice" style="margin-right:1.4em"><%= notice %></p>
                  <p class="alert" style="margin-right:1.4em"><%= alert %></p>
                </div>
              </div>
            </div>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </div>

  </body>
</html>
<script>
  /*
    document.addEventListener('DOMContentLoaded', () => {
        (document.querySelectorAll('.notification .delete') || []).forEach(($delete) => {
            const $notification = $delete.parentNode;

            $delete.addEventListener('click', () => {
                $notification.parentNode.removeChild($notification);
            });
        });
    });

   */
</script>
