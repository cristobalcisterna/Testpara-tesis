<%= form_with(model: duty_manager, local: true) do |form| %>
  <% if duty_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(duty_manager.errors.count, "error") %> prohibited this duty_manager from being saved:</h2>

      <ul>
        <% duty_manager.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_duty_manager %>
    <%= form.text_field :name_duty_manager %>
  </div>



  <div class="field">
    <%= form.label :rut %>
    <%= form.text_field :rut %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
