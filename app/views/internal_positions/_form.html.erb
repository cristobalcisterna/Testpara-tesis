<%= form_with(model: internal_position, local: true) do |form| %>
  <% if internal_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(internal_position.errors.count, "error") %> prohibited this internal_position from being saved:</h2>

      <ul>
        <% internal_position.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :is_real %>
    <%= form.check_box :is_real %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
